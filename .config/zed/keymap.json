// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    // Keep the VSCode style cmd-ctrl-{left|right} to move panes left and right as
    // the default split duplicates them
    "context": "Workspace || Editor",
    "bindings": {
      "cmd-ctrl-left": [
        "workspace::MoveItemToPaneInDirection",
        { "direction": "left" }
      ],
      "cmd-ctrl-right": [
        "workspace::MoveItemToPaneInDirection",
        { "direction": "right" }
      ]
    }
  },
  {
    "bindings": {
      // Swap the bindings for open settings UI and open settings file as I only
      // want to edit the JSON file with cmd+,
      "cmd-,": "zed::OpenSettingsFile",
      "cmd-alt-,": "zed::OpenSettings"
    }
  }
]
