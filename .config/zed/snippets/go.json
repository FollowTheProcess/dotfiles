{
  "error handling": {
    "prefix": "iferr",
    "description": "Insert an err != nil check",
    "body": [
      "if err != nil {",
      "\treturn $1, fmt.Errorf(\"$2: %w\", err)",
      "    }"
    ]
  }
}
